export const days = ["monday", "tuesday", "wednesday", "thursday", "friday", "saturday", "sunday"]

export const TOKEN_GEN_DAYS_COUNT = 3

export enum TokenStates {
	ACTIVE = 1,
	ACCEPTED = 2,
	EXPIRED = 3,
	REJECTED = 4,
}
